version: '3.8'
#启动命令 docker compose -p flashcode -f docker-compose-mid.yml up -d
services:
  flashcode-mysql:
    container_name: flashcode-mysql
    image: mysql:8.4.2
    ports:
      - "3306:3306"
    volumes:
      # 数据挂载
      - ../data/flashcodedata/mysql/data:/var/lib/mysql
      # 配置挂载
      - ./mysql/init/init.sql:/docker-entrypoint-initdb.d/init.sql
      - ./mysql/sql:/opt/sql
    command: [
      'mysqld',
      '--innodb-buffer-pool-size=512M',
      '--character-set-server=utf8mb4',
      '--collation-server=utf8mb4_unicode_ci',
      '--default-time-zone=+8:00',
      '--lower-case-table-names=1',
      '--explicit_defaults_for_timestamp=true',
      '--server-id=1',
      '--log-bin=mysql-bin',
      '--binlog-ignore-db=mysql',
      '--binlog-cache-size=256M',
      '--binlog-format=ROW',
      '--max_connections=2000'
    ]
    restart: always
    networks:
      - flashcode_network_prd
    healthcheck:
      test: mysql --user=zmbdpprd -h127.0.0.1 --password='Hf@173503494' -e "SELECT 1;"
      start_period: 30s
      interval: 10s
      timeout: 5s
      retries: 10
    environment:
      TZ: Asia/Shanghai
      MYSQL_ROOT_PASSWORD: Hf@173503494
    ulimits:
      nproc: 65535
      nofile:
        soft: 20000
        hard: 40000
  flashcode-nacos:
    container_name: flashcode-nacos
    image: nacos/nacos-server:v2.2.2
    environment:
      - MODE=standalone
      - JVM_XMS=1g
      - JVM_XMX=1g
      - JVM_XMN=512m
      - SPRING_DATASOURCE_PLATFORM=mysql
      - MYSQL_SERVICE_HOST=flashcode-mysql
      - MYSQL_SERVICE_PORT=3306
      - MYSQL_SERVICE_DB_NAME=flashcode_nacos_prd
      - MYSQL_SERVICE_USER=zmbdpprd
      - MYSQL_SERVICE_PASSWORD=Hf@173503494
    volumes:
      - ../data/flashcodedata/nacos/logs/:/home/nacos/logs
      - ../data/flashcodedata/nacos/tomcatlogs/:/home/bit/nacos/tomcat/logs/
      - ./nacos/conf/application.properties:/home/nacos/conf/application.properties
    ports:
      - "8848:8848"
      - "9848:9848"
      - "9849:9849"
    restart: always
    networks:
      - flashcode_network_prd
    depends_on:
      flashcode-mysql:
        condition: service_healthy
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8848/nacos" ]
      start_period: 30s
      interval: 10s
      timeout: 10s
      retries: 10
  flashcode-redis:
    container_name: flashcode-redis
    # 带有布隆过滤器版本的 redis 镜像
    image: redislabs/rebloom:latest
    # 普通版本 redis 镜像
    #    image: redis:7.0.15
    ports:
      - "6379:6379"
    volumes:
      - ./redis/conf/redis.conf:/usr/local/etc/redis/redis.conf
      - ../data/flashcodedata/redis/data:/data
    command: redis-server /usr/local/etc/redis/redis.conf
    restart: always
    networks:
      - flashcode_network_prd
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 10
  flashcode-rabbitmq:
    image: rabbitmq:3.12.6-management
    container_name: flashcode-rabbit
    restart: always
    hostname: flashcode-rabbit
    environment:
      # 用户名
      RABBITMQ_DEFAULT_USER: zmbdpprd
      # 密码
      RABBITMQ_DEFAULT_PASS: Hf@173503494
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - ../data/flashcodedata/rabbit/log:/var/log/rabbit
      - ../data/flashcodedata/rabbit/data:/var/lib/rabbit
    networks:
      - flashcode_network_prd
    healthcheck:
      test: [ "CMD", "rabbitmqctl", "status" ]
      interval: 10s
      timeout: 10s
      retries: 10
  
  flashcode-web:
    image: nginx:1.24.0
    container_name: flashcode-web
    restart: always
    networks:
      - flashcode_network_prd
    ports:
      - "80:80"
    volumes:
      - ./nginx/conf/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/web/dist:/data/flashcode/web/dist
    ulimits:
      nproc: 65535
      nofile:
        soft: 20000
        hard: 40000
  
  # ==================== 链路追踪 SkyWalking ====================
  flashcode-skywalking-oap:
    container_name: flashcode-skywalking-oap
    build:
      context: ./skywalking
      dockerfile: Dockerfile
    image: flashcode-skywalking-oap:9.7.0-mysql
    ports:
      - "11800:11800"  # gRPC 端口（Agent 上报数据）
      - "12800:12800"  # HTTP 端口（UI 查询数据）
    environment:
      TZ: Asia/Shanghai
      SW_STORAGE: mysql
      SW_JDBC_URL: jdbc:mysql://flashcode-mysql:3306/flashcode_skywalking_prd?serverTimezone=Asia/Shanghai&characterEncoding=utf8
      SW_DATA_SOURCE_USER: zmbdpprd
      SW_DATA_SOURCE_PASSWORD: Hf@173503494
      SW_HEALTH_CHECKER: default
      SW_TELEMETRY: prometheus
      JAVA_OPTS: -Xms1g -Xmx1g
    volumes:
      - ../data/flashcodedata/skywalking/oap/logs:/skywalking/logs
    restart: always
    networks:
      - flashcode_network_prd
    depends_on:
      flashcode-mysql:
        condition: service_healthy
    healthcheck:
      test: [ "CMD-SHELL", "curl -f http://localhost:12800/internal/l7check || exit 1" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  flashcode-skywalking-ui:
    container_name: flashcode-skywalking-ui
    image: apache/skywalking-ui:9.7.0
    ports:
      - "8080:8080"
    environment:
      TZ: Asia/Shanghai
      SW_OAP_ADDRESS: http://flashcode-skywalking-oap:12800
      SW_ZIPKIN_ADDRESS: http://flashcode-skywalking-oap:9412
    restart: always
    networks:
      - flashcode_network_prd
    depends_on:
      flashcode-skywalking-oap:
        condition: service_healthy

  # ==================== 服务监控 Prometheus + Grafana ====================
  flashcode-prometheus:
    container_name: flashcode-prometheus
    image: prom/prometheus:v2.48.0
    user: "0:0"
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - ./prometheus/rules:/etc/prometheus/rules
      - ../data/flashcodedata/prometheus/data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=90d'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
      - '--web.enable-lifecycle'
    restart: always
    # Prd 环境：服务部署在 Docker 容器，使用 Docker 网络通信
    networks:
      - flashcode_network_prd
    healthcheck:
      test: [ "CMD", "wget", "-q", "--spider", "http://localhost:9090/-/healthy" ]
      interval: 10s
      timeout: 5s
      retries: 5

  flashcode-grafana:
    container_name: flashcode-grafana
    image: grafana/grafana:10.2.2
    user: "0:0"
    ports:
      - "3000:3000"
    environment:
      TZ: Asia/Shanghai
      GF_SECURITY_ADMIN_USER: zmbdpprd
      GF_SECURITY_ADMIN_PASSWORD: Hf@173503494
      GF_INSTALL_PLUGINS: grafana-clock-panel,grafana-simple-json-datasource,grafana-piechart-panel
      GF_DEFAULT_LOCALE: zh-Hans
    volumes:
      - ../data/flashcodedata/grafana/data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning
      - ./grafana/dashboards:/var/lib/grafana/dashboards
    restart: always
    networks:
      - flashcode_network_prd
    depends_on:
      - flashcode-prometheus
    healthcheck:
      test: [ "CMD", "wget", "-q", "--spider", "http://localhost:3000/api/health" ]
      interval: 10s
      timeout: 5s
      retries: 5

  flashcode-alertmanager:
    container_name: flashcode-alertmanager
    image: prom/alertmanager:v0.26.0
    user: "0:0"
    ports:
      - "9093:9093"
    volumes:
      - ./alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml
      - ../data/flashcodedata/alertmanager/data:/alertmanager
    command:
      - '--config.file=/etc/alertmanager/alertmanager.yml'
      - '--storage.path=/alertmanager'
    restart: always
    networks:
      - flashcode_network_prd
    healthcheck:
      test: [ "CMD", "wget", "-q", "--spider", "http://localhost:9093/-/healthy" ]
      interval: 10s
      timeout: 5s
      retries: 5

  # ==================== 定时任务调度 XXL-JOB ====================
  flashcode-xxljob-admin:
    container_name: flashcode-xxljob-admin
    image: xuxueli/xxl-job-admin:2.4.2
    ports:
      - "8081:8080"
    environment:
      TZ: Asia/Shanghai
      PARAMS: >-
        --spring.datasource.url=jdbc:mysql://flashcode-mysql:3306/flashcode_xxljob_prd?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&serverTimezone=Asia/Shanghai
        --spring.datasource.username=zmbdpprd
        --spring.datasource.password=Hf@173503494
        --xxl.job.accessToken=flashcode_prd
        --server.servlet.context-path=/xxl-job-admin
    volumes:
      - ../data/flashcodedata/xxljob/logs:/data/applogs
    restart: always
    networks:
      - flashcode_network_prd
    depends_on:
      flashcode-mysql:
        condition: service_healthy
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8080/xxl-job-admin/actuator/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

networks:
  flashcode_network_prd:
